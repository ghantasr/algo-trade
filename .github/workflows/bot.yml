name: Run Algo Trade Bot

on:
  schedule:
    # Run every 2 hours on weekdays to cover full NSE trading day
    # with overlap so there's no gap between runs
    # Times in UTC (IST = UTC + 5:30):
    # 2:30 AM UTC = 8:00 AM IST (pre-market)
    # 4:30 AM UTC = 10:00 AM IST (market open)
    # 6:30 AM UTC = 12:00 PM IST
    # 8:30 AM UTC = 2:00 PM IST
    # 10:30 AM UTC = 4:00 PM IST (post-market)
    - cron: '30 2,4,6,8,10 * * 1-5'
  workflow_dispatch:

concurrency:
  group: algo-trade-bot
  cancel-in-progress: false

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 115  # ~1h55m (under 2h, so next cron overlaps)

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Download persisted data from previous run
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          name: algo-trade-data
          path: data/

      - name: Run bot with data persistence
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          CHECK_INTERVAL: 120
          INVESTMENT_PER_TRADE: 10000
          TARGET_PROFIT_PCT: 5
          MAX_PROFIT_PCT: 10
          STOP_LOSS_PCT: 2
          EXIT_MINUTES_BEFORE_CLOSE: 60
          MAX_POSITIONS: 3
          MAX_BUY_SUGGESTIONS: 2
          MIN_GAIN_PCT: 0.5
          PREDICTION_LOOKBACK_DAYS: 20
          PREMARKET_ONLY: false
          INTERVAL: 5m
          SHORT_MA: 9
          LONG_MA: 21
          CANDIDATES_FILE: data/daily_candidates.json
          PORTFOLIO_FILE: data/portfolio.json
        run: |
          set -e
          
          echo "🤖 Starting Algo Trade Bot..."
          echo "Market: NSE (India)"
          echo "Check interval: ${CHECK_INTERVAL}s"
          echo "Max runtime: 115 minutes"
          echo ""
          
          mkdir -p data
          
          # Trap SIGTERM (sent by GitHub when timeout approaches) to save data
          _cleanup() {
            echo ""
            echo "📦 Saving data before shutdown..."
            ls -la data/ 2>/dev/null || true
            echo "✅ Data saved. Bot will resume on next cron trigger."
            exit 0
          }
          trap _cleanup SIGTERM SIGINT
          
          # Run the bot
          python main.py
          
          # If bot exits naturally, still save data
          echo "📦 Bot finished. Saving data..."
          ls -la data/ 2>/dev/null || true

      - name: Upload data for next run
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: algo-trade-data
          path: data/
          retention-days: 1
          overwrite: true
